# ACHADOS — tabela-mestre do ciclo WebQA

Regras (ver `README.md`): ID estável `QA-NNNN` (nunca reutilizado);
`corrigido` exige commit + teste de regressão + laudo antes/depois;
`aceito` exige justificativa em `DECISOES.md`; `regua` = defeito da
própria suíte (proposta de correção em texto, issue no qa-suite —
**nunca** commit na suíte).

| ID | Severidade | Perfil/Origem | Alvo | Sintoma | Estado | Fix (commit) | Teste de regressão | Laudo a/d |
|---|---|---|---|---|---|---|---|---|
| QA-0001 | alta | backend/e2e + curl | `crates/audio_api/src/routes/mod.rs` (api_router) | `PUT /api/v1/uploads/{key}` com >2 MB retorna **413** (default do axum); nginx prod aceita 100 MB (`client_max_body_size`) — **upload real quebrado em produção**; via proxy vite vira 502 | em-correcao | — | — | laudos/backend-antes.txt + curl |
| QA-0002 | alta | backend (test_security_headers) | origem servida (vite dev) + docs/18 (nginx prod) | Sem `X-Content-Type-Options: nosniff`, sem `X-Frame-Options`/`frame-ancestors`, sem CSP. HSTS existe só no nginx prod (docs/18), ausente na origem dev | corrigido | (este commit) | suíte `test_security_headers.py` 6/6 PASSED | laudos/qa-0002-0007-antes-depois.txt |
| QA-0003 | média | backend (test_resposta_comprimida) | origem servida (vite dev) + docs/18 | HTML servido sem compressão gzip (dev); vhost nginx de produção também não declara gzip | corrigido | (este commit) | suíte `test_resposta_comprimida` PASSED | laudos/qa-0002-0007-antes-depois.txt |
| QA-0004 | média | backend (test_404_tratado) | `ui/vite.config.ts` (appType spa fallback) | Rota inexistente devolve **200** com o shell da SPA (fallback) — 404 não é tratado; **e pior**: `/healthz` na origem dev devolve 200 fake (HTML) em vez do health real | corrigido | (este commit) | suíte `test_404_tratado_sem_vazamento` PASSED + `test_endpoint_de_saude_existe` PASSED legit | laudos/qa-0002-0007-antes-depois.txt |
| QA-0005 | média | backend (test_correlacao xfail) + docs/03 §1 | `crates/audio_api` (middleware) + `ui/vite.config.ts` (plugin) | Contrato docs/03: "cliente envia `traceparent` (W3C); servidor devolve no response" — **não implementado** (extractor existe, eco não). Sem header de correlação em ambas as origens | corrigido | 4e619a5 (api) + 5405556 (vite plugin) | `cargo test -p audio_api --test qa_contracts` (4/4) + `cargo test -p audio_api --lib` (11 em `middleware::trace`) + suíte `test_correlacao_de_requisicoes` PASSED | laudos/qa-0005-antes-depois.txt |
| QA-0006 | média | backend (test_erro_estruturado, skip) + docs/03 §1 | `crates/audio_api` (fallback de erro) | Contrato docs/03: erros `application/problem+json` (RFC 7807) — 404 de /api/* devolve corpo **vazio**; handlers devolvem texto puro | corrigido | 4e619a5 | `cargo test -p audio_api --test qa_contracts` (6/6) + `cargo test -p audio_api --lib` (6 em `problem`) + suíte `test_erro_de_api_e_estruturado` PASSED | laudos/qa-0006-antes-depois.txt |
| QA-0007 | baixa | backend (test_endpoint_de_saude) | `ui/vite.config.ts` (proxy) | `/healthz` não é proxyado na origem dev — check de saúde passa por engano via fallback SPA (QA-0004); com 404 corrigido, health real precisa ser exposto (nginx prod já expõe) | corrigido | (este commit) | suíte `test_endpoint_de_saude_existe` PASSED legit (não mais via SPA fallback) | laudos/qa-0002-0007-antes-depois.txt |
| QA-0008 | régua | backend (test_https_e_usado) | suíte: checks/backend/test_http_basics.py:17 | Check exige HTTPS incondicional; alvo local autorizado é `http://localhost` (loopback). Falha sem ser defeito do alvo | regua | — (não se corrige no mixlirous) | — | laudos/backend-antes.txt |
| QA-0009 | régua | instalação da suíte | suíte: webqa-suite/requirements.txt | Linha corrompida `httpxttp2]>=0.27` (esperado `httpx[http2]>=0.27`) quebra `pip install -r requirements.txt` | regua | — (contorno local documentado) | — | logs de instalação |
| QA-0010 | média | inspeção de código (durante fix QA-0001) | 52 arquivos `.rs` do workspace | Mojibake sistêmico (UTF-8→cp850/latin-1 dupla codificação): travessões `ÔÇö`, acentos `├ó` — comentários e mensagens ilegíveis | corrigido | (próximo commit) | `cargo test --workspace` 430/430 verdes (validação de compilação+testes pós-reparo) | varredura de assinaturas: 0 resíduos |
| QA-0011 | baixa | frontend (test_html_quality.py::test_uso_de_html_semantico) | `ui/index.html` | Home SPA sem tags semânticas (header/nav/main/footer) — só `<div id=root>`. Dificulta acessibilidade e manutenção | corrigido | (este commit) | suíte `test_uso_de_html_semantico` PASSED (4 tags: header, nav, main, footer) | laudos/qa-0011-0012-antes-depois.txt |
| QA-0012 | média | lgpd (test_transparencia.py::test_politica_acessivel) + LGPD Art. 9º | `ui/public/politica.html` + link em `ui/index.html` | Sem link para política de privacidade na home — LGPD Art. 9º (comunicação ao titular sobre o tratamento) descumprido | corrigido | (este commit) | suíte `test_politica_acessivel` + `test_direitos_do_titular` + `test_canal_encarregado` PASSED | laudos/qa-0011-0012-antes-depois.txt |
| QA-0013 | baixa | lgpd (test_retencao_observavel.py::test_permissions_policy) | `ui/vite.config.ts` (securityHeadersPlugin) | Sem `Permissions-Policy` — câmera/microfone/geolocalização ficam disponíveis a qualquer script. Privilégio mínimo por default ausente (LGPD minimização) | corrigido | (este commit) | suíte `test_permissions_policy` PASSED (era XFAIL) | laudos/qa-0013-0014-antes-depois.txt |
| QA-0014 | baixa | lgpd (test_retencao_observavel.py::test_security_txt) + RFC 9116 | `ui/public/.well-known/security.txt` | Sem `/.well-known/security.txt` com campo `Contact:` (RFC 9116). Canal público de reporte ausente — apoia dever de comunicar incidente (LGPD Art. 48) | corrigido | (este commit) | suíte `test_security_txt` PASSED (era XFAIL) | laudos/qa-0013-0014-antes-depois.txt |
