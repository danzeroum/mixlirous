# module-status — Mixlirous
# Atualizar após cada tarefa concluída
#
# Item D1 do mapa de ação: status agora reflete integração real, não
# apenas code-complete. Cada módulo tem duas colunas: `code` (a peça
# existe?) e `integration` (a peça está conectada à ponta a ponta?).

audio_core: code 100%, integration 100%
  domain: MFCC real, JobRecord, PipelineConfig
  dsp/analysis: onset, beat_tracking (limiar adaptativo #27), rms, fft, chroma+seções
  dsp/stitching: crossfade, fades, zero_cross
  dsp/mastering: compressor, lufs (limited_samples #37), limiter, stretch
  dsp/selection: knapsack DP, continuous
  dsp/post_process: invariantes I1 (clipping) + I4 (estalos)
  ports: AudioRepo completo (claim/beat/retry)

audio_agent: code 95%, integration 80%
  react_kernel: ReAct c/ budget, retry, propostas HITL, replan, observações, fallback, streaming (A1-A10)
  llm: MockLlm (stream+errors+sequence), Ollama adapter, OpenAI adapter
  prompt_loader: minijinja templating (render_prompt + RenderedPrompt)
  prompt_guard: regex + 4096 limit
  validator+limits: completos (pré-existente)
  # INTEGRATION GAP (fechado por B1 nesta iteração):
  # ReActOutput agora serializa (derive Serialize/Deserialize) e o worker
  # aplica a receita ao PipelineConfig via apply_recipe_to_config.
  # Próximo passo: system_prompt hardcoded em build_llm_request deve vir
  # do prompt_loader (item M2 do relatório — não fechado).

audio_api: code 95%, integration 80%
  routes:
    jobs: create/list/get + download_artifact (B4) + cancel REAL (C6, Lote 2) +
      retry (C12 parcial, Lote 2)
    tracks: create/get/list + peaks REAL (C9, Lote 2) + raw (Lote 2, player A/B)
    uploads: presign + PUT
    proposals: list/approve/reject/replan (store morto — B5 pendente)
    prompts: list/get (lê catalog.json)
    tools: list (espelha limits::tool_registry)
    tenants: quota + consent (mock)
    system: info (sem resources nem scale)
    sse: job_stream (sem Last-Event-ID — B7 pendente) + handshake autenticado
      por cookie de sessão same-origin (issue #33, Lote 2; rotas em
      routes/auth.rs: local-session + sse-session)
    dev_slice: diagnóstico (gated MIXLIROUS_DEV_SLICE=1)
  sse: EventHub broadcast conectado ao job_stream
  adapters: InMemoryRepo, SqliteRepo
  worker: pipeline DSP real + heartbeat + callbacks SSE (agent.thought/error/replan)
    # Lote 2: worker cancel-aware — job cancelado não é sobrescrito por
    # completed/failed e não volta para a fila via fail_and_retry
    # B1 FECHADO: apply_recipe_to_config aplica tool_calls ao PipelineConfig
    # B4 FECHADO: download_url agora aponta para /api/v1/jobs/{id}/artifact
    # C4 FECHADO: erro do agente vira agent.error estruturado em vez de panic
    # M7 FECHADO: job.warning segue schema do contrato (code/severity/hint)
  middleware: auth JWT, rate_limit
  resiliência: atomic_write, recovery loop

ui: code 95%, integration 85%
  # Lote 1 do plano Pareto (+5): ToolPalette respeita available=false de
  # GET /tools — ghost tools (compression, dynamic_eq) desabilitadas com
  # motivo no tooltip; lógica em lib/toolFilter.ts com testes Vitest.
  components:
    UploadPanel: modo manual/assisted selecionável (B3)
    ProposalOverlay: parâmetros editáveis antes de aceitar (C3)
    ToolPalette (NOVO): paleta de ferramentas de GET /tools com filtro de
      disponibilidade (Lote 1 item 4)
    RemixCanvas: DAG React Flow (nós estáticos — pendente integração com
      graph do backend quando get_job retornar JobResponse completo)
    Player: player A/B comparativo (C2) + original via track_id
      (GET /tracks/{id}/raw, Lote 2 item 2 — sem upload manual)
  hooks:
    useSSE (NOVO): addEventListener para eventos nomeados (B2);
      useParamStream virou re-export para compat; ensureSseSession()
      antes do handshake (issue #33, Lote 2)
    useApi: ApiRequestError estruturado (C1) + approveProposal com body
  store: graphStore (Zustand) + addToolNode (Lote 1)
  types: api.ts reescrito com PipelineConfig mirror do Rust (B3) +
    defaultPipelineConfig() helper + ApiError RFC 7807

sprints:
  S1: 100% — fila, persistência, rotas, SSE, worker
  S2: 100% — compressor, knapsack, chroma, MFCC, worker↔DSP
  S3: 100% — ReAct, LlmProvider, prompt_guard, HITL, frontend (backend tasks 3.1-3.11)
  S4: 100% — atomic_write, recovery, heartbeat, rate_limit, métricas
  # Sprint de integração (esta iteração): B1, B2, B3, B4, C1, C2, C3, C4 — fechados
  # Lote 1 do plano Pareto: docs sincronizados (README/08/14), CI
  # incondicional em PR (#5), redes do compose restauradas, paleta com
  # filtro de ghost tools (item 4).
  # Próxima sprint (pendentes): B5 (HITL store populado), B7 (Last-Event-ID replay),
  # C4 do relatório (JobResponse completo em get_job), C9 (peaks TODO),
  # C12 (9 endpoints documentados mas não implementados)

tests: 436+ (199 core + 71 agent + 96 api + 4 e2e + 4 contract_ts_rust + 9 ui/vitest)
build: OK
clippy: OK (-D warnings)
fmt: OK (--check)

# Histórico de mudanças (detalhes em CHANGELOG.md):
# 2026-08-20: B1 (recipe aplicada), B2 (SSE hook fix), B3 (contract sync),
#             B4 (artifact endpoint), C1-C4 (UI), T1/T2/T3 (tests),
#             D1-D4 (docs)
# Lote 2 Pareto: C9 (peaks), C6 (cancel), C12-parcial (retry), #33 (SSE
#             auth via cookie), raw por track_id, save_job com JobMeta
#             (divergência adendo §1.7)
# Lote 1 Pareto: docs (adendo §1), CI incondicional (#5), compose [mixlirous],
#             ToolPalette + toolFilter (ui 80→85)
